openapi: 3.1.0
info:
  title: marketplace-deployment
paths:
  /tags/{resourceArn}:
    DELETE:
      summary: 'Untag Resource '
      description: <p>Removes a tag or list of tags from a resource.</p>
      tags:
        - ARN
        - Resources
        - Tags
        - Untag
  /catalogs/{catalog}/products/{productId}/deployment-parameters:
    POST:
      summary: 'Put Deployment Parameter '
      description: >-
        <p>Creates or updates a deployment parameter and is targeted by
        <code>catalog</code> and <code>agreementId</
      tags:
        - Catalog
        - Catalogs
        - Deployments
        - Parameters
        - Products
